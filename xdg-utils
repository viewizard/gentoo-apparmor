# ------------------------------------------------------------------
#
#  Copyright (C) 2016,2017 Mikhail Kurinnoi
#
#  This program is free software; you can redistribute it and/or
#  modify it under the terms of version 2 of the GNU General Public
#  License published by the Free Software Foundation.
#
# ------------------------------------------------------------------

#include <tunables/global>

profile xdg-utils.xdg-open /usr/bin/xdg-open flags=(attach_disconnected) {
  #include <abstractions/base>
  #include <abstractions/dbus/session-strict>
  #include <abstractions/freedesktop.org>
  #include <abstractions/nameservice>
  
  # PSEUDO ---------------------------------------------
  /dev/tty						rw,
  /dev/tty[0-9]*					rw,
  /dev/pts/[0-9]*					rw,
  owner @{PROC}/@{pid}/fd/				r,
  
  # EXECUTABLES ----------------------------------------
  /usr/bin/xdg-open					r,
  @{shell}						mr,
  /bin/grep						Cx,
  /bin/egrep						Cx,
  /usr/bin/exo-open					Px,
  /usr/bin/dbus-send					Px,
  audit /usr/bin/xprop					Px,	# FIX ME! audit 26.06.2017
  
  # DENY INHERIT ---------------------------------------
  deny unix peer=(label="wps-office"),				# wps-office
  deny unix peer=(label="qpdfview"),				# qpdfview
  deny unix peer=(label="evelauncher"),				# evelauncher
  deny unix peer=(label="teamspeak-client-bin.teamspeak"),	# teamspeak-client-bin.teamspeak
  deny network netlink raw,					# skype
  deny network inet dgram,					# teamspeak-client-bin.teamspeak
  deny network inet6 dgram,					# teamspeak-client-bin.teamspeak
  deny network inet stream,					# evelauncher
  deny network inet6 stream,					# evelauncher
  deny /dev/shm/.org.chromium.Chromium.*		ra,	# evelauncher
  deny /apparmor/.null					rw,	# flags=(attach_disconnected)
  deny /usr/share/qt[0-9]*/**				r,	# teamspeak-client-bin.teamspeak
  deny /usr/share/pyfa/imgs.zip				r,	# pyfa
  deny /run/user/[0-9]*/poedit.lock			w,	# poedit
  deny /tmp/skype-[0-9]*/DbTemp./temp-*			rw,	# skype
  deny @{HOME}/.Skype/**				rw,	# skype
  deny @{HOME}/.local/share/poedit/TranslationMemory/*	rw,	# poedit
  deny @{HOME}/.kingsoft/office6/**			rw,	# wps-office
  deny @{HOME}/.ts3client/**				rw,	# teamspeak-client-bin.teamspeak
  deny @{HOME}/evelauncher/**				r,	# evelauncher
  deny @{HOME}/.local/share/CCP/**			rw,	# evelauncher
  deny @{HOME}/.pyfa/Pyfa-[0-9]*.log			w,	# pyfa
  deny @{HOME_CACHE}/CCP/**				rw,	# evelauncher
  
  profile grep /bin/grep {
    #include <abstractions/base>
    
    # EXECUTABLES --------------------------------------
    /bin/grep						mr,
  }
  
  profile egrep /bin/egrep {
    #include <abstractions/base>
    
    # EXECUTABLES --------------------------------------
    /bin/egrep						r,
    @{shell}						mr,
    /bin/grep						ix,	# Все правильно, для egrep используем с ix.
  }
}

profile xdg-utils.xdg-email /usr/bin/xdg-email {
  #include <abstractions/base>
  
  # PSEUDO ---------------------------------------------
  /dev/tty						rw,
  /dev/tty[0-9]*					rw,
  /dev/pts/[0-9]*					rw,
  
  # EXECUTABLES ----------------------------------------
  /usr/bin/xdg-email					r,
  @{shell}						mr,
  /bin/sed						Cx,
  /usr/bin/which					Px,
  /usr/bin/exo-open					Px,
  
  profile sed /bin/sed {
    #include <abstractions/base>
    
    # PSEUDO -------------------------------------------
    /dev/tty[0-9]*					rw,
    /dev/pts/[0-9]*					rw,
    
    # EXECUTABLES --------------------------------------
    /bin/sed						mr,
  }
}

profile xdg-utils.xdg-screensaver /usr/bin/xdg-screensaver {
  #include <abstractions/base>
  #include <abstractions/dbus/session-strict>
  #include <abstractions/X>
  
  # PSEUDO ---------------------------------------------
  /dev/tty						rw,
  /dev/tty[0-9]*					rw,
  /dev/pts/[0-9]*					rw,
  
  # EXECUTABLES ----------------------------------------
  /usr/bin/xdg-screensaver				r,
  @{shell}						mr,
  /bin/grep						Cx,
  /bin/mv						Cx,
  /bin/sed						Cx,
  /bin/hostname						Px,
  /usr/bin/which					Px,
  /usr/bin/dbus-send					Px,
  /usr/bin/xset						ix,	# FIX ME! Вынести в отдельный профиль.
  /usr/bin/xautolock					Px,
  
  profile grep /bin/grep {
    #include <abstractions/base>
    
    # PSEUDO -------------------------------------------
    /dev/tty[0-9]*					rw,
    /dev/pts/[0-9]*					rw,
    
    # EXECUTABLES --------------------------------------
    /bin/grep						mr,
  }
  
  profile mv /bin/mv {
    #include <abstractions/base>
    
    # PSEUDO -------------------------------------------
    /dev/tty[0-9]*					rw,
    /dev/pts/[0-9]*					rw,
    
    # EXECUTABLES --------------------------------------
    /bin/mv						mr,
  }
  
  profile sed /bin/sed {
    #include <abstractions/base>
    
    # PSEUDO -------------------------------------------
    /dev/tty[0-9]*					rw,
    /dev/pts/[0-9]*					rw,
    
    # EXECUTABLES --------------------------------------
    /bin/sed						mr,
  }
}

# Первоначально был дочерним профилем chromium, тестировать с chromium.
# BUG! (модуль v3.6) При запуске xdg-settings в chromium с Px, выводит
# в kernel.log дамп на AA_BUG: security/apparmor/file.c:136 aa_audit_file
# если в настройках хромиума попытаться установить хромиум браузером
# по умолчанию (в любом случае, в XFCE4 такой метод не работает).
profile xdg-utils.xdg-settings /usr/bin/xdg-settings {
  #include <abstractions/base>
  #include <abstractions/freedesktop.org>
  #include <abstractions/fonts>
  #include <abstractions/gtk>
  
  # PSEUDO ---------------------------------------------
  /dev/tty						rw,
  /dev/tty[0-9]*					rw,
  /dev/pts/[0-9]*					rw,
  
  # EXECUTABLES ----------------------------------------
  /usr/bin/xdg-settings					mr,
  # Checking default browser
  @{shell}						mr,
  /bin/grep						Cx,
  /bin/readlink						Cx,
  /bin/sed						Cx,
  /bin/mkdir						Cx,
  /bin/mv						Cx,
  /bin/touch						Cx,
  /bin/cut						Cx,
  /bin/dirname						Px,
  /usr/bin/which					Px,
  /usr/bin/basename					Px,
  /usr/bin/gconftool-2					Px,	# Detect proxy settings for gnome2/gnome3.
  /usr/bin/gawk						Cx,
  /usr/bin/xdg-mime					Px,
  
  # READS/WRITES ---------------------------------------
  /etc/xdg/xfce4/helpers.rc				r,
  owner @{HOME}/.config/xfce4/helpers.rc		r,
  owner @{HOME}/.local/share/applications/		w,
  owner @{HOME}/.local/share/applications/mimeapps.list* rw,
  
  # NOISY ----------------------------------------------
  deny @{shell}						x,	# checking default browser
  
  profile grep /bin/grep {
    #include <abstractions/base>
    
    # PSEUDO -------------------------------------------
    /dev/tty[0-9]*					rw,
    /dev/pts/[0-9]*					rw,
    
    # EXECUTABLES --------------------------------------
    /bin/grep						mr,
    
    # READS/WRITES -------------------------------------
    owner @{HOME}/.config/xfce4/helpers.rc		r,
  }
  
  profile readlink /bin/readlink {
    #include <abstractions/base>
    
    # EXECUTABLES --------------------------------------
    /bin/readlink					mr,
  }
  
  profile sed /bin/sed {
    #include <abstractions/base>
    
    # EXECUTABLES --------------------------------------
    /bin/sed						mr,
  }
  
  profile mkdir /bin/mkdir {
    #include <abstractions/base>
    
    # EXECUTABLES --------------------------------------
    /bin/mkdir						mr,
  }
  
  profile mv /bin/mv {
    #include <abstractions/base>
    
    # EXECUTABLES --------------------------------------
    /bin/mv						mr,
  }
  
  profile touch /bin/touch {
    #include <abstractions/base>
    
    # EXECUTABLES --------------------------------------
    /bin/touch						mr,
  }
  
  profile cut /bin/cut {
    #include <abstractions/base>
    
    # PSEUDO -------------------------------------------
    /dev/tty[0-9]*					rw,
    /dev/pts/[0-9]*					rw,
    
    # EXECUTABLES --------------------------------------
    /bin/cut						mr,
  }
  
  profile gawk /usr/bin/gawk {
    #include <abstractions/base>
    
    # EXECUTABLES --------------------------------------
    /bin/gawk						mr,
  }
}

profile xdg-utils.xdg-mime /usr/bin/xdg-mime {
  #include <abstractions/base>
  
  # EXECUTABLES ----------------------------------------
  /usr/bin/xdg-mime					mr,
}

profile xdg_open_root {
  #include <abstractions/base>
  #include <abstractions/dbus/session-strict>
  #include <abstractions/freedesktop.org>
  #include <abstractions/nameservice>
  #include <abstractions/X>
  
  # PSEUDO ---------------------------------------------
  /dev/tty						rw,
  /dev/tty[0-9]*					rw,
  /dev/pts/[0-9]*					rw,
  owner @{PROC}/@{pid}/fd/				r,
  
  # EXECUTABLES ----------------------------------------
  /usr/bin/xdg-open					r,
  @{shell}						mr,
  /bin/uname						Px,
  /bin/grep						Cx,
  /bin/egrep						Cx,
  /usr/bin/exo-open					Px -> exo_open_root,
  /usr/bin/dbus-send					Px,
  audit /usr/bin/xprop					Px,	# FIX ME! audit 26.06.2017
  
  profile grep /bin/grep {
    #include <abstractions/base>
    
    # EXECUTABLES --------------------------------------
    /bin/grep						mr,
  }
  
  profile egrep /bin/egrep {
    #include <abstractions/base>
    
    # EXECUTABLES --------------------------------------
    /bin/egrep						r,
    @{shell}						mr,
    /bin/grep						ix,	# Все правильно, для egrep используем с ix.
  }
}

profile xdg_email_root {
  #include <abstractions/base>
  
  # PSEUDO ---------------------------------------------
  /dev/tty						rw,
  /dev/tty[0-9]*					rw,
  /dev/pts/[0-9]*					rw,
  
  # EXECUTABLES ----------------------------------------
  /usr/bin/xdg-email					r,
  @{shell}						mr,
  /bin/sed						Cx,
  /usr/bin/which					Px,
  /usr/bin/exo-open					Px -> exo_open_root,
  
  profile sed /bin/sed {
    #include <abstractions/base>
    
    # EXECUTABLES --------------------------------------
    /bin/sed						mr,
  }
}
