# ------------------------------------------------------------------
#
#  Copyright (C) 2017 Mikhail Kurinnoi
#
#  This program is free software; you can redistribute it and/or
#  modify it under the terms of version 2 of the GNU General Public
#  License published by the Free Software Foundation.
#
# ------------------------------------------------------------------

#include <local/tunables.d/>

profile dhcp.dhclient /sbin/dhclient {
  #include <abstractions/base>
  #include <abstractions/nameservice>
  
  # CAPABILITIES ---------------------------------------
  capability net_raw,
  capability net_bind_service,
  
  # NETWORK --------------------------------------------
  network packet raw,
  network inet dgram,
  network inet6 dgram,
  
  # PSEUDO ---------------------------------------------
  owner @{PROC}/@{pid}/net/dev				r,
  
  # EXECUTABLES ----------------------------------------
  /sbin/dhclient					mr,
  /usr/libexec/nm-dhcp-helper				Px,	# net-misc/networkmanager	# DELEGATION! networkmanager.NetworkManager
  
  # READS/WRITES ---------------------------------------
  /run/dhclient-*.pid					rw,	# net-misc/networkmanager	# DELEGATION! networkmanager.NetworkManager	# Зависит от конфигурации NetworkManager
  /var/lib/NetworkManager/dhclient-*.conf		r,	# net-misc/networkmanager	# DELEGATION! networkmanager.NetworkManager	# Зависит от конфигурации NetworkManager
  /var/lib/NetworkManager/dhclient-*.lease		rw,	# net-misc/networkmanager	# DELEGATION! networkmanager.NetworkManager	# Зависит от конфигурации NetworkManager
}
