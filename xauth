# ------------------------------------------------------------------
#
#  Copyright (C) 2016,2017 Mikhail Kurinnoi
#
#  This program is free software; you can redistribute it and/or
#  modify it under the terms of version 2 of the GNU General Public
#  License published by the Free Software Foundation.
#
# ------------------------------------------------------------------

#include <local/tunables.d/>

profile xauth /usr/bin/xauth {
  #include <abstractions/base>
  
  # PSEUDO ---------------------------------------------
  /dev/tty[0-9]*					rw,
  /dev/pts/[0-9]*					rw,
  
  # EXECUTABLES ----------------------------------------
  /usr/bin/xauth					mr,
  
  # USERS ----------------------------------------------
  owner @{HOME}/.Xauthority*				rw,	# * - 0(нет) или 2 символа {[a-z],-}
  owner link subset @{HOME}/.Xauthority*		-> /{home/*,root}/.Xauthority*,		# TARGET! @{HOME}	# * - 0(нет) или 2 символа {[a-z],-}
  owner @{HOME}/.serverauth.*				rw,	# * - 1-8 символов @{pid} + 0(нет) или 2 символа {[a-z],-}
  owner link subset @{HOME}/.serverauth.*		-> /{home/*,root}/.serverauth.*,	# TARGET! @{HOME}	# * - 1-8 символов @{pid} + 0(нет) или 2 символа {[a-z],-}
  owner @{HOME}/.xauth*					rw,	# * - 6-8 символов {[a-zA-Z0-9_.},-}
  owner link subset @{HOME}/.xauth*			-> /{home/*,root}/.xauth*,	# TARGET! @{HOME}	# * - 6-8 символов {[a-zA-Z0-9_.},-}
  
  # TEMP -----------------------------------------------
  owner /tmp/@{TMP_SH}					rw,
}
