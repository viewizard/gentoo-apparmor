# ------------------------------------------------------------------
#
#  Copyright (C) 2016,2017 Mikhail Kurinnoi
#
#  This program is free software; you can redistribute it and/or
#  modify it under the terms of version 2 of the GNU General Public
#  License published by the Free Software Foundation.
#
# ------------------------------------------------------------------

#include <tunables/global>

profile pam-script /etc/security/pam-script/pam_script* flags=(complain) {
  #include <abstractions/base>
  #include <abstractions/nameservice>
  
  # CAPABILITIES ---------------------------------------
  capability setuid,
  
  # PSEUDO ---------------------------------------------
  /dev/tty						rw,
  /dev/tty[0-9]*					rw,
  /dev/pts/[0-9]*					rw,
  
  # EXECUTABLES ----------------------------------------
  /etc/security/pam-script/pam_script*			r,
  @{shell}						Cxmr,	# Все правильно, запуск и mmap.
  /bin/dirname						Px,
  /bin/basename						Px,
  
  # READS/WRITES ---------------------------------------
  /etc/security/pam-script/pam-script.d/{,**} r,
  
  # USERS ----------------------------------------------
  @{HOME}/.session.log					w,	# XFCE4
  
  profile shell @{shell} flags=(complain) {
    #include <abstractions/base>
    #include <abstractions/nameservice>
    
    # PSEUDO -------------------------------------------
    /dev/tty						rw,
    /dev/tty[0-9]*					rw,
    /dev/pts/[0-9]*					rw,
    
    # EXECUTABLES --------------------------------------
    /etc/security/pam-script/pam-script.d/pam_script*	r,
    @{shell}						mr,
    
    # USERS --------------------------------------------
    owner @{HOME}/.session.log				w,	# XFCE4, <abstractions/X>
  }
}
