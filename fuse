# ------------------------------------------------------------------
#
#  Copyright (C) 2016,2017 Mikhail Kurinnoi
#
#  This program is free software; you can redistribute it and/or
#  modify it under the terms of version 2 of the GNU General Public
#  License published by the Free Software Foundation.
#
# ------------------------------------------------------------------

#include <local/tunables.d/>

profile fuse.fusermount /usr/bin/fusermount {
  #include <abstractions/base>
  #include <abstractions/nameservice>
  
  # CAPABILITIES ---------------------------------------
  capability sys_admin,
  capability dac_override,
  capability dac_read_search,
  
  # UNIX -----------------------------------------------
  unix (send, receive)
  			type=stream
			addr=none
  			peer=(addr=none,label="gvfs.gvfsd-fuse"),
  
  # MOUNT ----------------------------------------------
  mount fstype="fuse.gvfsd-fuse" options=(ro, nosuid, nodev) gvfsd-fuse -> /root/.gvfs/,
  mount fstype="fuse.gvfsd-fuse" options=(rw, nosuid, nodev) gvfsd-fuse -> /root/.gvfs/,
  mount fstype="fuse.gvfsd-fuse" options=(ro, nosuid, nodev) gvfsd-fuse -> /run/user/[0-9]*/gvfs/,
  mount fstype="fuse.gvfsd-fuse" options=(rw, nosuid, nodev) gvfsd-fuse -> /run/user/[0-9]*/gvfs/,
  umount /root/.gvfs/,
  umount /run/user/[0-9]*/gvfs/,
  
  # PSEUDO ---------------------------------------------
  /dev/fuse						rw,
  @{PROC}/@{pid}/mounts					r,	# IMPROVE! UID=$USER(owner),root(owner)
  
  # EXECUTABLES ----------------------------------------
  /usr/bin/fusermount					mr,
  
  # READS/WRITES ---------------------------------------
  /etc/fuse.conf					r,
  /run/user/[0-9]*/gvfs/				r,
  
  # USERS ----------------------------------------------
  owner /root/.gvfs/					r,
}
